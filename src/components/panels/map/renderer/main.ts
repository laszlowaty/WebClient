import { Map } from "leaflet";
import { MapArea } from "../../../../store/types";

// Renders the map to leaflet
export const renderMapToleaflet = (map: Map, area: MapArea) => {
  //
  // tutaj ogarnij rysowanie roomów z "area" na mapce "map"
  //
}